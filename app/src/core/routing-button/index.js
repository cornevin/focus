 import React from 'react';
import RaisedButton from 'material-ui/RaisedButton'

const style = {
  margin: 12,
}

const RoutingButton = () => (
  <div>
    <RaisedButton label="Join the focus Room"  style={style} />
  </div>
)

export default RoutingButton
